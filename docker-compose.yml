services:
  app:
    image: wahh220899/devicetag:latest
    container_name: devicetag_app
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
